#include <stdio.h>
/* Copy input to output; 2nd version */
int main(void)
{
  int c; //int is used because char is not big enough for EOF
  while((c = getchar()) != EOF)
    {
      putchar(c);
    }
}
